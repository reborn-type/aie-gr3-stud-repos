# HW14 – эмбеддинги, FAISS, оценка retrieval и mini-RAG по базе знаний

## 1. Кратко: что сделано

- Какая база знаний использовалась и почему.
    Использовалась искусственая база знаний, сделанная прямо в блокноте, для прозрачности документов и 
    лучшего понимания где и что находиться
- Как был организован чанкинг документов.
    чанками размером 22 с помощью функции chunking_text
- Какая embedding-модель или какой способ векторизации использовался.
    embedding-модель: TfidfFallbackBackend, как самая простая 
- Что показала базовая проверка retrieval.
    
    metric	value
0	mean_hit@3	0.80
1	mean_recall@3	0.80
2	mean_MRR@3	0.75

- Что изменилось после обновления базы знаний.
    У одного из документов появилось сходство с нужным документом, а у второго - нет, документы
    поменялись местами.
- Как сработал mini-RAG.
    metric	value
0	mean_hit@3	0.90
1	mean_recall@3	0.90
2	mean_MRR@3	0.85

## 2. Среда и воспроизводимость

- Python: 3.11.13
- faiss / faiss-cpu: True  
- sentence-transformers / transformers / sklearn: -
- torch (если использовался): 2.8
- Устройство (CPU/GPU): cpu
- Seed: 43
- Как запустить: открыть `HW14.ipynb` и выполнить Run All.

## 3. База знаний и подготовка документов

- Предметная область: технологии в frontend разработке
- Источник документов: google.com, habr.com 
- Число исходных документов: 30
- Число чанков после разбиения: 41
- Какой способ чанкинга использовался: разбиение текста на чанки длиной до 25
- Основные параметры чанкинга: chunking_size - 25, overlap - 10 
- Комментарий (3-6 предложений): База знаний является абсолютно искусственной, сделанной собственноручно, что заметно упрощает эксперименты с mini-RAG, поскольку есть возможность отреадктировать выбранный документ и посмотреть что произойдет, а также упрощает тем, что все документы содержат в себе конкретные текста из одной сферы и не обобщаются.

## 4. Retrieval и индекс

### 4.1. Эмбеддинги и индекс

- Какая embedding-модель или способ векторизации использовались: 
- Какая метрика сходства использовалась:
- Какой индекс `FAISS` использовался:
- Какой `top_k` использовался по умолчанию:
- Что показали первые примеры retrieval:

### 4.2. Контрольные запросы и оценка retrieval

- Сколько контрольных запросов использовалось: embedding-модель: TfidfFallbackBackend
- Что считалось релевантным ответом: правильно выбранный документ с ответом по теме запроса
- Какие метрики retrieval считались: hit, recall, first_relevant_rank, mrr
- Итоговый `hit@k`: 0.80
- Итоговый `recall@k`: 0.80
- Дополнительные метрики (если были): first_relevant_rank, mrr
- Какие запросы оказались самыми простыми: Те запросы, у которых большое и явное совподение в тексте с текстом документа
- Какие запросы оказались самыми проблемными: Запросы, отличавшиеся по написанию, но совпадающие со смыслом, либо очень неоднозначные запросы на которые можно дать несколько ответов

## 5. Эксперимент по параметрам retrieval и обновление базы знаний

Опишите коротко и по делу:

- Какой параметр сравнивался (`chunk_size`, `overlap`, `top_k` или другой разумный вариант): chunk_size, overlap, top_k
- Какие два варианта сравнивались: 1 = [chunk_size=25, overlap=10, top_k=3], 2 = [chunk_size=10, overlap=3, top_k=5]
- Как изменилось качество retrieval: не изменилось
- Какой вариант вы выбрали как основной и почему: первый, потому что метрики особо не отличились
- Что именно было обновлено в базе знаний: два документа
- Как была выполнена переиндексация: через documents
- Что изменилось в retrieval после обновления базы знаний: выросли метрики

## 6. Mini-RAG и результаты

Ссылки на файлы в репозитории:

- Оценка retrieval: `./artifacts/retrieval_eval.csv`
- Примеры ответов mini-RAG: `./artifacts/rag_examples.csv`
- Сравнение до/после обновления: `./artifacts/retrieval_before_after_update.csv`

Короткая сводка (6-10 строк):

- Как формировался контекст для ответа: была пройдена очистка пустых строк, удаление меток, разбиение не предложения, и пул контекста
- Какой способ генерации ответа использовался: generate_answer_from_context
- Возвращались ли источники вместе с ответом: да 
- На каких вопросах mini-RAG сработал лучше всего: на вопросе о том как работает cors
- На каких вопросах mini-RAG ошибался или отвечал неуверенно: что такое сео аналитика как работает ассинхронность и замыкания в джаваскрипт
- Какую роль сыграло качество retrieval в итоговом ответе: небольшое 

## 7. Анализ

(8-15 предложений)

Нужно прокомментировать:

- насколько удачной оказалась выбранная база знаний;
    Была удачной, но можно было прописать подробнее для лучшего распознования. 
- как выбор чанкинга повлиял на retrieval;
    Не сильно 
- насколько полезной оказалась формальная оценка retrieval;
    Были заметны слабые стороны 
- что показал эксперимент с параметрами;
    Метрики не изменились
- как обновление базы знаний повлияло на результаты;
    Улучшило один запрос 
- какие ошибки mini-RAG оказались наиболее показательными;
    Где был выбран абсолютно не совпадающий отрезок документа
- какие ограничения у текущего учебного решения.
    Для решения нужны очень подробные базы знаний, плохо считывает контекст, иногда напрямую не может найти сходства

## 8. Итоговый вывод

(3-7 предложений)

- Какой retrieval-конвейер вы бы оставили как базовый для такой задачи.
    Для подобной учебной задачи я бы оставил классический retrieval-конвейер: query -> embedding -> vector search -> top-k docs
- Что главное вы поняли про качество retrieval.
    Качество retrieval сильно зависит от embedding, и сильно влияет на качество итогового ответа
- Что главное вы поняли про связь retrieval и итогового качества mini-RAG.
    retrieval важен для итогового качества mini-RAG, поскольку используется меньщий объем данных
    и следовательно каждая потеря важных фрагментов, будет сильно отражаться на итоговом качестве 

## 9. Приложение (опционально)

Если вы делали дополнительные сравнения:

- сравнение двух embedding-моделей;
- дополнительный benchmark по `top_k`;
- более подробный анализ ошибок;
- дополнительные графики или таблицы.
